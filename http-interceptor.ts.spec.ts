import { HttpInterceptor.Ts } from './http-interceptor.ts';

describe('HttpInterceptor.Ts', () => {
  it('should create an instance', () => {
    expect(new HttpInterceptor.Ts()).toBeTruthy();
  });
});
